# Coding Agent Harness 项目反思

> 学术规范说明：本文由 Codex 根据负责人在项目中的真实选择、逐项审批、审阅意见、Git 记录和验证结果起草，并由 Codex 辅助整理、校对与润色。负责人需在课程提交前亲自通读、核对并按本人理解修改；文中的取舍和结论均对应实际项目过程，不把未进行的实验写成已完成。

## 从“不知道该做什么”到确定主线

开始项目时，我没有 coding agent harness 的相关经验，甚至不清楚六个维度应该怎样落到代码中。Brainstorming 对我最大的帮助不是直接给出功能列表，而是迫使我逐项确定边界：项目采用 Python-first，CLI 为主要控制面，WebUI 只做只读报告；LLM 使用 OpenAI-compatible 接口，凭据进入操作系统 Keyring；文件修改采用精确替换，命令采用 `shell=False` 的结构化子进程；状态使用 SQLite，审计使用追加式 JSONL。最重要的决定是把治理作为主要贡献，并采用 ALLOW、REQUIRE_APPROVAL、DENY 三级风险策略。经过这一过程，我才理解“治理”不是一句要求模型谨慎的提示词，而是 Policy Gateway、审批状态机、动作指纹、工作区漂移检查和单次消费票据组成的确定性机制。

Brainstorming 也有形式大于实质的部分。早期几乎每个问题都采用三选一并逐节确认，在四天时间盒里消耗了很多轮次。它还一度过度强调 WebUI，后来才收缩为 CLI 主体加最小静态查看器。我认为架构、安全边界和数据模型值得逐项确认，但命名、目录等低风险且可逆的细节可以在规则确定后采用保守默认值，不必全部升级为产品决策。

## TDD 与评审的实际作用

在 AI 协作下，TDD 对我不是阻碍，而是放大器。没有红灯时，智能体很容易把“写出了代码”等同于“机制已经成立”；先写失败测试后，目标被压缩成一个可以判断真假的合同。例如治理策略最初只针对少量已知危险参数做拒绝，质量审查很快发现 `pytest --basetemp`、Ruff 写入参数、mypy 报告输出和 `@参数文件` 都可能绕过只读命令边界。后来实现改成每种工具的显式安全参数语法，未知选项默认拒绝。这个过程说明单纯增加 denylist 不足以证明安全，真正有效的是测试、规格复审和攻击性质量复审共同形成闭环。

TDD 也让我看到测试本身会出错。Windows 临时目录 ACL 曾让 pytest 在 fixture 阶段失败，这不是产品 RED；README 修改也曾破坏交付测试要求的固定短语。最终只读审查又在 371 项全绿后发现跨会话审批复用、终态恢复产生副作用、工作区锁未接入入口三个集成缺口。为它们补出真实红灯后，我更确定测试数量不能代替风险分析；模块合同、攻击性探针和真实入口测试缺一不可。最终测试仍保持离线、可重复且不读取真实 Keyring 或真实 LLM。

## Subagent、Task 颗粒度与上下文

Subagent-driven 工作流最适合“一组稳定接口、一个主要模块、一组明确测试”的任务。Task 2 的模型、Task 6 的文件工具、Task 8 的记忆等范围相对清楚，子智能体能独立完成红灯、实现和定向验证。Task 5 同时包含命令策略、审批、指纹、预算、迁移和并发，颗粒度过大，后续经历了多轮规格与质量修复。我的结论是，最优 task 应能在一个上下文中完整回答“输入是什么、失败怎样表现、由哪组测试验收”，并控制在一次独立提交内；跨越多个安全边界的任务应继续拆分。

最有效的 context 策略是“有界且按需”：实现者只获得当前 Task 对应的 SPEC、PLAN 合同、稳定接口和相关文件，评审者则使用新上下文重新检查。失败时传递精确命令、错误输出和工作树状态，而不是复制全部对话历史。这样既减少偏题，也让审查不被实现者的解释带偏。项目没有按原计划执行 Claude Code 异构冷启动，只进行了 fresh Codex 的受限只读审查；我认为这只能证明文档可读性，不能冒充异构工具的真实实现试验。

## SPEC、PLAN 与工程要求带来的改变

SPEC 和 PLAN 的质量直接决定实现质量。一个具体案例是早期 PLAN 只写“验证命令白名单”，没有明确安全参数语法，执行者自然选择了较粗的前缀匹配，导致原生工具参数可以产生文件写入或联网副作用。把合同改成“已知安全选项允许、未知选项拒绝、路径必须留在工作区”后，实现和测试才真正一致。另一个例子是 Composition Root 最初只组装了空 mock，没有把已经完成的分层配置、Keyring 和 OpenAI-compatible 客户端接入正式 `cah run`。收尾时新增端到端测试才发现这一点，也发现批准后的动作没有在 `sessions resume` 中继续执行。这提醒我：单模块全部通过不等于用户流程已经闭环，PLAN 应更早加入从 CLI 到持久层再到工具执行的纵向测试。

凭据要求迫使我区分“配置”和“秘密”。provider URL、model 和 profile 可以写 TOML，API Key 只能通过隐藏输入进入 Keyring，不能出现在命令行、日志、配置、报告和 Git 历史中。分发要求则让我第一次认真处理 wheel/sdist、包内静态资源、双平台 mypy、CI artifact、Pages 最小权限和可安装性验证。Linux CI 暴露了 Windows 本地没有出现的字节码缓存与平台常量问题，说明“本机测试通过”远远不等于可分发。

## 如果重做与方法论批判

如果重做，我会在 Task 3 完成配置模型后立刻写 Composition Root 的端到端测试，在 Task 5 完成审批后立刻测试“暂停、重启、批准、恢复、单次执行”的完整 CLI 流程；同时提前建立 README、许可证清单和反思提纲，避免最后集中补文档。我还会把 Task 5 拆成策略判定与持久审批两个任务，并尽早安排一次真正的异构智能体冷启动。

我对 Superpowers 的总体评价是：它擅长把隐含决定变成可检查的 SPEC、测试和评审门禁，但它假设时间充足、任务可以干净拆分、工作树稳定，而且测试能够代表真实风险。本项目中的 Windows 权限、网络波动、远端 CI 差异和跨模块组装缺口都不完全满足这些假设。因此，方法论不能机械执行。应保留 brainstorming、TDD、独立评审和完成前验证这些实质纪律，同时压缩重复确认，对环境失败先做系统调试，并用端到端证据校验模块化计划没有遗漏真实用户流程。
